#!/bin/bash

# Usage instructions
if [ -z "$1" ]; then
  echo "Usage: $0 {IP_ADDRESS}"
  exit 1
fi

# Ping the IP 5 times
for i in {1..5}; do
  ping -c 1 "$1" >/dev/null 2>&1
  if [ $? -eq 0 ]; then
    echo "Ping to $1 was successful"
  else
    echo "Ping to $1 failed"
  fi
  sleep 1
done
